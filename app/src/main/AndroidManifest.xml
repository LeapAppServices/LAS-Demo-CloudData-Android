<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="as.leap.demo.clouddata">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.CoreActivity" />
        <activity android:name=".activities.DataTypeActivity" />
        <activity android:name=".activities.FileActivity" />
        <activity android:name=".activities.GeoActivity" />
        <activity android:name=".activities.OperationActivity" />
        <activity android:name=".activities.QueryActivity" />
        <activity android:name=".activities.RoleActivity" />
        <activity android:name=".activities.SubClassActivity" />
        <activity android:name=".activities.UserActivity" />
    </application>

</manifest>
